import React from 'react'
import PropTypes from 'prop-types'

function Film({data}) {
  return (
    <div>
      
    </div>
  )
}

Film.propTypes = {
  data:PropTypes.object.isRequired,
};

export default Film

